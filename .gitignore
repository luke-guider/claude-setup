# Palace data — too large, not for git
mempalace/palace/
mempalace/hook_state/
mempalace/knowledge_graph.sqlite3

# Per-machine config
config/paths.sh
*.local

# OS cruft
.DS_Store
._.DS_Store

# Pre-install backups created during install
*.pre-install-backup

# Business logic / internal architecture (not for public)
claude/context/

# Per-user mempalace config — live files live in ~/.mempalace/, repo only ships .example.* templates
mempalace/identity.txt
mempalace/config.json
mempalace/wing_config.json
mempalace/mine/*.sh
!mempalace/mine/*.example.sh
